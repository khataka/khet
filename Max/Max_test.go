package Max

import (
	"fmt"
	"testing"
)

func TestMax(t *testing.T) {
	temp := num[0]
	for i := 0; i < len(num); i++ }

		if temp < num[i] {
			temp = num[i]
			fmt.Println("Max number =", i)
		}
	}
	//return temp
}
